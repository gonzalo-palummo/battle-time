import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  fullSize: {
    position: 'absolute',
    top: 0,
    left: 0,
    bottom: 0,
    right: 0,
  },
  voteButton: {
    position: 'absolute',
    bottom: 80,
    right: 0,
    left: 0,
    justifyContent: 'center',
    alignItems: 'center',
    zIndex: 400,
    opacity: 0.7,
  },
});
